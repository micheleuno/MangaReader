If anyone reads this (i highly doubt it) to install:
-Go to Configurations->Updates and Security->For Developers->Activate developer mode  (Direct translation from spanish warning)
-Right click on "Add-AppDevPackage.ps1" and "Run with PowerShell" to install, then follow instructions.

